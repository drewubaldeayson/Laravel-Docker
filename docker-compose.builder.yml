version: '3.8'
services:
  humite-backend:
    image: takamata018/laravel7.4-fpm
    restart: unless-stopped
    container_name: humite-backend
    volumes:
      - ${DOCUMENT_ROOT}:/var/www/html
    ports:
      - ${APP_MACHINE_SECURE_HOST_PORT}:9000
    networks:
      - ${NETWORK}
  nginx:
    image: nginx:stable-alpine
    restart: unless-stopped
    container_name: humite-backend-nginx
    ports:
      - ${HOST_MACHINE_UNSECURE_HOST_PORT}:80
      - ${HOST_MACHINE_SECURE_HOST_PORT}:443
    volumes:
      - ${DOCUMENT_ROOT}:/var/www/html
      - ${NGINX_CONFIG_DIR}:/etc/nginx/conf.d/default.conf
      - ${NGINX_LOG_DIR}:/var/log/nginx
    depends_on:
      - humite-backend
    networks:
      - ${NETWORK}

  # db:
  #   image: mysql
  #   container_name: humite-backend-mysql
  #   restart: always
  #   command: --lower_case_table_names=2
  #   ports:
  #     - ${DATABASE_HOST_MACHINE_HOST_PORT}:3306
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${MY_DATABASE}
  #     MYSQL_USER: ${MYSQL_USER}
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  #   volumes:
  #     - ./containerization/humite-be-db-data:/var/lib/mysql

  # install:
  #   extends:
  #     service: humite-backend
  #   command: composer install
  # build:
  #   extends:
  #     service: humite-backend
  #   command: bash -c "chmod -R 777 storage && php artisan key:generate"
  # migrate:
  #   extends:
  #     service: humite-backend
  #   command: php artisan migrate

networks:
  humite-backend:
    name: ${NETWORK}

# volumes:
#   humite-be-db-data:
#     external: true